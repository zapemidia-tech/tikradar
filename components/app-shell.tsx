'use client';
import type{ReactNode}from'react';import{useEffect,useState}from'react';import{BarChart3,Bell,Box,Compass,Heart,LayoutDashboard,Search,Settings,Store,Users,Video,TriangleAlert,Sparkles}from'lucide-react';
const links=[['/',LayoutDashboard,'Dashboard'],['/products',Box,'Produtos'],['/radar',Compass,'Radar de Oportunidades'],['/creators',Users,'Criadores'],['/shops',Store,'Lojas'],['/videos',Video,'Vídeos'],['/categories',BarChart3,'Categorias'],['/favorites',Heart,'Favoritos'],['/alerts',TriangleAlert,'Alertas'],['/settings',Settings,'Configurações']] as const;
export function AppShell({children,active}:{children:ReactNode;active?:string}){const[source,setSource]=useState({label:'Dados demonstrativos',lastSync:null as string|null});useEffect(()=>{fetch('/api/data-source/status').then(r=>r.json()).then((x:unknown)=>{const s=x as {label:string;lastSync?:{finished_at?:string}};setSource({label:s.label,lastSync:s.lastSync?.finished_at??null})}).catch(()=>{})},[]);return <main className="app-shell"><aside className="sidebar"><a className="brand" href="/"><span className="brand-mark"><Sparkles size={17}/></span><span>TikRadar</span></a><p className="eyebrow">INTELIGÊNCIA</p><nav>{links.map(([href,Icon,label])=><a className={active===href?'nav-item active':'nav-item'} href={href} key={href}><Icon size={18}/><span>{label}</span>{href==='/radar'&&<b>12</b>}</a>)}</nav><div className="sidebar-foot"><div className="legal-links"><a href="/privacy">Privacidade</a><a href="/security">Segurança</a><a href="/data-requests">Dados</a></div><div className="user"><span>AG</span><div><strong>Adriel Godoy</strong><small>Plano Pro</small></div></div></div></aside><section className="content"><header className="topbar"><div className="search"><Search size={17}/><span>Buscar produtos, lojas ou criadores...</span><kbd>⌘ K</kbd></div><button className="icon-button" aria-label="Notificações"><Bell size={18}/><i/></button><span className="live" title={source.lastSync?'Última sincronização disponível':'Nenhuma sincronização real'}><i/> {source.label}</span></header>{children}</section></main>}
export function PageTitle({eyebrow,title,subtitle,children}:{eyebrow:string;title:string;subtitle:string;children?:ReactNode}){return <div className="title-row"><div><p className="eyebrow green">{eyebrow}</p><h1>{title}</h1><p>{subtitle}</p></div>{children}</div>}
export{brl,compact}from'@/lib/format';
