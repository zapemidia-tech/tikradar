'use client';import{brl,compact}from'./app-shell';import type{Creator,Shop,Video}from'@/types';
export function CreatorsTable({items}:{items:Creator[]}){return <article className="panel table-panel"><div className="table-wrap"><table><thead><tr><th>CRIADOR</th><th>SEGUIDORES</th><th>VENDAS</th><th>GMV</th><th>PRODUTOS</th><th>VIEWS</th><th>ENGAJAMENTO</th><th>CRESCIMENTO</th></tr></thead><tbody>{items.slice(0,25).map((x,i)=><tr key={x.id}><td><span className={'avatar a'+i%4}>{x.name.slice(0,2)}</span><div><strong>{x.name}</strong><small>{x.username}</small></div></td><td>{compact(x.followers)}</td><td>{compact(x.sales)}</td><td>{brl(x.gmv)}</td><td>{x.products}</td><td>{compact(x.views)}</td><td>{x.engagement.toFixed(1)}%</td><td><span className="growth">↗ {x.growth}%</span></td></tr>)}</tbody></table></div></article>}
export function ShopsTable({items}:{items:Shop[]}){return <article className="panel table-panel"><div className="table-wrap"><table><thead><tr><th>LOJA</th><th>CATEGORIA</th><th>PRODUTOS ATIVOS</th><th>VENDAS</th><th>GMV</th><th>CRESCIMENTO</th><th>AVALIAÇÃO</th><th>CRIADORES</th></tr></thead><tbody>{items.map((x,i)=><tr key={x.id}><td><span className={'avatar a'+i%4}>{x.name.slice(0,2)}</span><div><strong>{x.name}</strong><small>Loja verificada</small></div></td><td><span className="tag">{x.category}</span></td><td>{x.activeProducts}</td><td>{compact(x.sales)}</td><td>{brl(x.gmv)}</td><td><span className="growth">↗ {x.growth}%</span></td><td>★ {x.rating.toFixed(1)}</td><td>{x.creators}</td></tr>)}</tbody></table></div></article>}
export function VideosTable({items}:{items:Video[]}){return <article className="panel table-panel"><div className="table-wrap"><table><thead><tr><th>VÍDEO / PRODUTO</th><th>CRIADOR</th><th>VIEWS</th><th>ENGAJAMENTO</th><th>VENDAS ATRIBUÍDAS</th><th>GMV</th><th>POSTADO</th><th>CRESCIMENTO</th></tr></thead><tbody>{items.slice(0,30).map((x,i)=><tr key={x.id}><td><span className={'video-thumb v'+i%4}>▶</span><div><strong>{x.product}</strong><small>{compact(x.likes)} curtidas · {compact(x.shares)} compartilhamentos</small></div></td><td>{x.creator}</td><td>{compact(x.views)}</td><td>{((x.likes+x.comments+x.shares)/x.views*100).toFixed(1)}%</td><td>{compact(x.sales)}</td><td>{brl(x.gmv)}</td><td>{x.date}</td><td><span className="growth">↗ {x.growth}%</span></td></tr>)}</tbody></table></div></article>}
