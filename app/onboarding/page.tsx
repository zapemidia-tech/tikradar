'use client';import{useState}from'react';import{BriefcaseBusiness,Search,Sparkles,Store,Users}from'lucide-react';export default function Onboarding(){const[step,setStep]=useState(1);const[selected,setSelected]=useState<string[]>([]);const opts=[['Sou afiliado',Users],['Sou vendedor',Store],['Sou agência',BriefcaseBusiness],['Faço pesquisa de produtos',Search]] as const;const cats=['Beleza','Casa','Eletrônicos','Moda','Acessórios','Saúde e bem-estar','Automotivo','Ferramentas','Esportes','Pets'];return <main className="onboarding"><a className="brand" href="/"><span className="brand-mark"><Sparkles/></span>TikRadar</a><div className="steps"><span className="done">1</span><i/><span className={step===2?'done':''}>2</span></div><section><p className="eyebrow green">PASSO {step} DE 2</p><h1>{step===1?'Qual é o seu objetivo?':'Quais categorias você acompanha?'}</h1><p>{step===1?'Isso nos ajuda a personalizar seus insights.':'Selecione quantas quiser. Você pode alterar depois.'}</p>{step===1?<div className="option-grid">{opts.map(([x,Icon])=><button className={selected.includes(x)?'active':''} onClick={()=>setSelected([x])} key={x}><Icon/><strong>{x}</strong><span>Recomendações pensadas para seu perfil.</span></button>)}</div>:<div className="category-select">{cats.map(x=><button className={selected.includes(x)?'active':''} onClick={()=>setSelected(s=>s.includes(x)?s.filter(v=>v!==x):[...s,x])} key={x}>{x}</button>)}</div>}<button className="continue" onClick={()=>step===1?(setSelected([]),setStep(2)):location.href='/'} disabled={!selected.length}>Continuar →</button></section></main>}
