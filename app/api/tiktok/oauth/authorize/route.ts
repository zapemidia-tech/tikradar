import{NextResponse}from'next/server';import{createSellerAuthorizationUrl}from'@/lib/tiktok/auth';import{getTikTokConfig}from'@/lib/tiktok/config';import{getChatGPTUser}from'@/app/chatgpt-auth';
export async function GET(){if(!(await getChatGPTUser()))return NextResponse.json({error:'Autenticação necessária.'},{status:401});const state=crypto.randomUUID().replaceAll('-','');const response=NextResponse.redirect(createSellerAuthorizationUrl(getTikTokConfig(),state));response.cookies.set('tiktok_oauth_state',state,{httpOnly:true,secure:process.env.NODE_ENV==='production',sameSite:'lax',maxAge:600,path:'/'});response.headers.set('cache-control','no-store');return response}
