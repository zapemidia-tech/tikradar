import{NextResponse}from'next/server';import{getDataSourceStatus}from'@/lib/tiktok/config';import{createClient}from'@supabase/supabase-js';
export async function GET(){const status=getDataSourceStatus();let lastSync:null|{finished_at:string|null;status:string;error_message:string|null;products_received:number;creators_received:number;videos_received:number}=null;const url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.SUPABASE_SERVICE_ROLE_KEY;if(url&&key){const{data}=await createClient(url,key,{auth:{persistSession:false}}).from('data_sync_runs').select('finished_at,status,error_message,products_received,creators_received,videos_received').order('started_at',{ascending:false}).limit(1).maybeSingle();lastSync=data}return NextResponse.json({...status,lastSync},{headers:{'cache-control':'no-store'}})}
