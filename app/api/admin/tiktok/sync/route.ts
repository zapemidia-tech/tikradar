import{NextResponse}from'next/server';import{createTikTokSyncService}from'@/services/tiktok/create-sync-service';import{getTikTokConfig}from'@/lib/tiktok/config';
async function digest(value:string){const bytes=await crypto.subtle.digest('SHA-256',new TextEncoder().encode(value));return Array.from(new Uint8Array(bytes)).join(',')}
async function authorized(request:Request){const expected=getTikTokConfig().adminSyncSecret,received=request.headers.get('x-admin-sync-token');return Boolean(expected&&received&&(await digest(expected))===(await digest(received)))}
export async function POST(request:Request){if(!(await authorized(request)))return NextResponse.json({error:'Não autorizado.'},{status:401});try{const counts=await (await createTikTokSyncService()).syncAll();return NextResponse.json({status:'success',counts})}catch(error){const message=error instanceof Error?error.message:'Erro desconhecido';const status=message.includes('Adapter Bestsellers')?422:503;return NextResponse.json({status:'failed',error:message},{status})}}
