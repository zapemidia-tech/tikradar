import{createClient,type SupabaseClient}from'@supabase/supabase-js';import type{BestsellersKind,BestsellersPeriod,NormalizedBestseller}from'@/lib/tiktok/types';import{TikTokConfigError}from'@/lib/tiktok/errors';
export interface SyncRepository{startRun(provider:string):Promise<string>;finishRun(id:string,status:'success'|'failed',counts:Record<BestsellersKind,number>,error?:string):Promise<void>;persist(kind:BestsellersKind,items:NormalizedBestseller[],period:BestsellersPeriod):Promise<void>}
export class SupabaseSyncRepository implements SyncRepository{private client:SupabaseClient;constructor(env:NodeJS.ProcessEnv=process.env){if(!env.NEXT_PUBLIC_SUPABASE_URL||!env.SUPABASE_SERVICE_ROLE_KEY)throw new TikTokConfigError('Supabase server-side não configurado.');this.client=createClient(env.NEXT_PUBLIC_SUPABASE_URL,env.SUPABASE_SERVICE_ROLE_KEY,{auth:{persistSession:false}})}async startRun(provider:string){const{data,error}=await this.client.from('data_sync_runs').insert({provider,status:'running'}).select('id').single();if(error)throw error;return data.id}async finishRun(id:string,status:'success'|'failed',counts:Record<BestsellersKind,number>,error?:string){const{error:e}=await this.client.from('data_sync_runs').update({finished_at:new Date().toISOString(),status,products_received:counts.products,creators_received:counts.creators,videos_received:counts.videos,lives_received:counts.lives,error_message:error?.slice(0,500)}).eq('id',id);if(e)throw e}async persist(kind:BestsellersKind,items:NormalizedBestseller[],period:BestsellersPeriod){if(!items.length)return;const entityTable=kind==='products'?'products':kind==='creators'?'creators':kind==='videos'?'videos':'lives';const snapshotTable=kind==='products'?'product_snapshots':kind==='creators'?'creator_snapshots':kind==='videos'?'video_snapshots':'live_snapshots';for(const item of items){const{data,error}=await this.client.from(entityTable).upsert({external_id:item.externalId,name:item.name??`${kind} ${item.externalId}`},{onConflict:'external_id'}).select('id').single();if(error)throw error;const foreignKey=kind==='products'?'product_id':kind==='creators'?'creator_id':kind==='videos'?'video_id':'live_id';const row:Record<string,unknown>={[foreignKey]:data.id,captured_at:new Date().toISOString(),ranking:item.ranking,period,gmv_min:item.gmv.gmvMin,gmv_max:item.gmv.gmvMax,gmv_estimated:item.gmv.gmvEstimated,raw_payload:item.rawPayload};if(kind==='products')Object.assign(row,{sold_count:item.soldCount,price:item.price,creator_count:item.creatorCount,video_count:item.videoCount,review_count:item.reviewCount,rating:item.rating});if(kind==='creators')Object.assign(row,{sales:item.soldCount,video_count:item.videoCount});if(kind==='videos')Object.assign(row,{views:item.views,sales:item.soldCount});const{error:snapshotError}=await this.client.from(snapshotTable).insert(row);if(snapshotError)throw snapshotError}}}
