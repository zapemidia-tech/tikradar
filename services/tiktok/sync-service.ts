import type{BestsellersAdapter}from'./adapters';import type{BestsellersService}from'./bestsellers-service';import type{SyncRepository}from'./sync-repository';import type{BestsellersKind,BestsellersPeriod,SyncCounts,TikTokCurrency}from'@/lib/tiktok/types';import{describeUnknownError}from'@/lib/tiktok/errors';
export class TikTokBestsellersSyncService{constructor(private service:BestsellersService,private adapter:BestsellersAdapter,private repo:SyncRepository,private currency:TikTokCurrency='LOCAL'){}private async sync(kind:BestsellersKind,period:BestsellersPeriod='7D'){try{const response=kind==='products'?await this.service.getBestsellingProducts({period,currency:this.currency}):kind==='creators'?await this.service.getBestsellingCreators({period,currency:this.currency}):kind==='videos'?await this.service.getBestsellingVideos({period,currency:this.currency}):await this.service.getBestsellingLives({period,currency:this.currency});const items=this.adapter.normalize(kind,response,this.currency);await this.repo.persist(kind,items,period);return items.length}catch(error){throw new Error(`Falha em ${kind}: ${describeUnknownError(error)}`,{cause:error})}}syncProducts(p?:BestsellersPeriod){return this.sync('products',p)}syncCreators(p?:BestsellersPeriod){return this.sync('creators',p)}syncVideos(p?:BestsellersPeriod){return this.sync('videos',p)}syncLives(p?:BestsellersPeriod){return this.sync('lives',p)}async syncAll(period:BestsellersPeriod='7D'){const runId=await this.repo.startRun('tiktok');const counts:SyncCounts={products:0,creators:0,videos:0,lives:0};try{counts.products=await this.syncProducts(period);counts.creators=await this.syncCreators(period);counts.videos=await this.syncVideos(period);counts.lives=await this.syncLives(period);await this.repo.finishRun(runId,'success',counts);return counts}catch(error){const message=describeUnknownError(error);try{await this.repo.finishRun(runId,'failed',counts,message)}catch(finishError){throw new Error(`${message} — Falha ao registrar diagnóstico: ${describeUnknownError(finishError)}`,{cause:error})}throw error}}}
